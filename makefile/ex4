CC = gcc
CFLAGS = -Wall -Werror -Wextra

ifeq ($(DEBUG), 1)
	CFLAGS += -g
endif

all: program

program: program.o
	$(CC) $(CFLAGS) program.o -o program 

program.o: program.c
	$(CC) $(CFLAGS) -c program.c
